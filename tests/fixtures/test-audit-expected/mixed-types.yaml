# Expected findings for mixed-types/everything.test.ts
# Used to validate test-audit pipeline output

classification:
  category: unit
  needs_deep_analysis: true
  deep_analysis_reason: "Single file contains multiple test types"

  risks:
    test_management:
      reason: "Single file contains unit, integration, and e2e tests"
      recommendation: "Split into separate files by test type"

detection:
  # No T1-T4 violations in this file - tests are well-written
  # The issue is organizational, not technical
  violations: []
  test_effectiveness: 100%

synthesis:
  REWRITE_REQUIRED: false
  gate_triggered: "Advisory only (no T1-T4 violations)"
  recommendations:
    - "Split into user-service.test.ts (unit tests)"
    - "Split into user-service.integration.ts (integration tests)"
    - "Split into user-registration.e2e.ts (e2e tests)"
