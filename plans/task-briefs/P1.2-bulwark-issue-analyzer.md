# P1.2 - Create bulwark-issue-analyzer Agent

**Task ID**: P1.2
**Phase**: P1 - Pipeline Stage Agents
**Status**: Ready for Implementation
**Created**: 2026-01-19
**Estimated Complexity**: Wrapper agent (15-20K tokens, single session)

---

## 1. Overview

### 1.1 Purpose

Create a custom sub-agent that analyzes issues to identify root cause, map impact, and produce a debug report with tiered validation plan. This agent is a **wrapper** around the `issue-debugging` skill, invoked as a single stage in the Fix Validation pipeline.

### 1.2 Pipeline Context

```fsharp
// Fix Validation Pipeline - Stage 1
IssueAnalyzer (bulwark-issue-analyzer, produces debug_report)  // <-- THIS AGENT
|> FixWriter (orchestrator implements fix)
|> TestWriter (orchestrator writes tests)
|> FixValidator (bulwark-fix-validator, validates against debug_report)
|> CodeReviewer (reviews all, approves/rejects)
|> LOOP(max=3)
```

### 1.3 Why a Wrapper Agent?

| Reason | Explanation |
|--------|-------------|
| **Single-stage operation** | No internal sub-agent spawning needed (unlike test-audit) |
| **Isolated context** | Bias avoidance - analyzes issue without having generated the code |
| **Encapsulated behavior** | Agent definition specifies skills, tools, output format |
| **Clean interface** | Orchestrator invokes via `Task(subagent_type="bulwark-issue-analyzer")` |

---

## 2. Agent Specification

### 2.1 Frontmatter

```yaml
---
name: bulwark-issue-analyzer
description: Analyzes issues to identify root cause, map impact, and produce debug report with tiered validation plan. Used as Stage 1 in Fix Validation pipeline.
agent: sonnet
skills:
  - issue-debugging
  - subagent-output-templating
tools:
  - Read
  - Grep
  - Glob
  - Write
  - Bash
---
```

### 2.2 Skills Loaded

| Skill | Purpose |
|-------|---------|
| `issue-debugging` | Core methodology - root cause analysis, impact mapping, validation plan, debug journey |
| `subagent-output-templating` | Output consistency - YAML schema, summary token budget, diagnostics format |

### 2.3 Tool Usage Constraints (Instruction-Based)

```markdown
## Tool Usage Constraints

### Write
- **Allowed**: `logs/debug-reports/`, `logs/diagnostics/`
- **Forbidden**: Source files, test files, config files, any file outside `logs/`

### Bash
- **Allowed**:
  - Read-only git commands (`git log`, `git blame`, `git diff`, `git show`)
  - Test execution for reproduction (`just test`, `npm test`, etc.)
  - File inspection (`ls`, `wc`, `file`)
  - Process inspection (`ps`, `lsof` for port checks)
- **Forbidden**:
  - Destructive commands (`rm`, `rmdir`, `mv`, `cp` to overwrite)
  - File modification (`sed -i`, `awk` with output redirect, `truncate`)
  - Git modifications (`git commit`, `git push`, `git reset`, `git checkout`)
  - Package installation (`npm install`, `pip install`)

### General
- **NEVER** modify source code, test files, or config files
- Analysis only - fixes are done by the orchestrator in subsequent pipeline stages
```

---

## 3. Output Specification

### 3.1 Debug Report

**Location**: `logs/debug-reports/{issue-id}-{YYYYMMDD-HHMMSS}.yaml`

**Schema** (from fix-validation.md):

```yaml
debug_report:
  metadata:
    issue_id: "{from CONTEXT or generated}"
    timestamp: "{ISO-8601}"
    analyzer: bulwark-issue-analyzer

  analysis:
    symptom: "{observable problem}"
    root_cause: "{underlying reason}"
    complexity: low | medium | high
    fix_approach: "{recommended fix direction}"

  impact_analysis:
    affected_files:
      - "{path}"
    upstream_dependencies:
      - "{what calls the affected code}"
    downstream_effects:
      - "{what the affected code impacts}"
    risk_scope: isolated | medium | broad

  validation_plan:
    tests_to_execute:
      - path: "{test file}"
        reason: "{why this test}"
        priority: 1  # P1=must, P2=should, P3=nice-to-have
    functionalities_to_validate:
      - "{user-visible functionality to verify}"

  confidence_criteria:
    high:
      - "{conditions for high confidence}"
    medium:
      - "{conditions for medium confidence}"
    low:
      - "{conditions for low confidence}"

  debug_journey:  # Required for medium/high complexity
    hypotheses_tested:
      - hypothesis: "{what was suspected}"
        result: confirmed | rejected
        evidence: "{supporting evidence}"
```

### 3.2 Diagnostics

**Location**: `logs/diagnostics/bulwark-issue-analyzer-{YYYYMMDD-HHMMSS}.yaml`

**Schema** (from subagent-output-templating):

```yaml
skill: bulwark-issue-analyzer
timestamp: "{ISO-8601}"
diagnostics:
  model_requested: sonnet
  model_actual: sonnet
  context_type: forked
  parent_vars_accessible: false
  hooks_fired: []
  execution_time_ms: 0
  completion_status: success | error | timeout
notes: "{any additional context}"
```

### 3.3 Summary (Return to Orchestrator)

**Token budget**: 100-200 tokens

**Format**:
```
Analyzed issue: {symptom}
Root cause: {root_cause} (complexity: {level})
Impact: {risk_scope} - {N} files affected
Validation plan: {M} tests (P1: {x}, P2: {y}, P3: {z})
Debug report: logs/debug-reports/{issue-id}-{timestamp}.yaml
```

---

## 4. Agent Content Structure

```markdown
# Bulwark Issue Analyzer

## Identity

You are an issue analysis specialist in the Bulwark quality system. Your role is to investigate bugs and issues to understand their root cause, map their impact, and produce a debug report that guides the fix implementation.

## Mission

**DO**:
- Analyze issues to identify root cause (not just symptoms)
- Map impact across upstream and downstream dependencies
- Produce structured debug report with tiered validation plan
- Document your debug journey (hypotheses tested, evidence gathered)
- Reproduce issues via test execution when possible

**DO NOT**:
- Modify any source code, test files, or config files
- Implement fixes (that's the orchestrator's job)
- Skip the validation plan (FixValidator depends on it)
- Write to any location outside `logs/`

## Protocol

### Step 1: Understand the Issue

Read the issue description from CONTEXT. Identify:
- Observable symptom
- Error messages / stack traces
- Reproduction steps (if available)

### Step 2: Investigate Root Cause

1. Locate relevant code using Grep/Glob
2. Read affected files
3. Trace execution path
4. Form hypotheses and test them
5. Use Bash for git history, test reproduction

### Step 3: Map Impact

Identify:
- Affected files (direct)
- Upstream dependencies (what calls this code)
- Downstream effects (what this code impacts)
- Risk scope (isolated / medium / broad)

### Step 4: Create Validation Plan

Tier tests by priority:
- **P1 (must)**: Direct tests of affected functionality
- **P2 (should)**: Integration tests of upstream callers
- **P3 (nice-to-have)**: E2E tests, edge cases

List functionalities that need manual validation if tests can't cover.

### Step 5: Define Confidence Criteria

Specify what constitutes high/medium/low confidence that the fix worked.

### Step 6: Write Outputs

1. Write debug report to `logs/debug-reports/{issue-id}-{YYYYMMDD-HHMMSS}.yaml`
2. Write diagnostics to `logs/diagnostics/bulwark-issue-analyzer-{YYYYMMDD-HHMMSS}.yaml`
3. Return summary to orchestrator (include debug report path)

## Tool Usage Constraints

[As specified in Section 2.3]

## Output Format

[Reference subagent-output-templating skill for YAML schema]

## Related Skills

- `issue-debugging` - Core methodology (loaded via frontmatter)
- `subagent-output-templating` - Output format (loaded via frontmatter)
```

---

## 5. Implementation Sub-Tasks

| ID | Task | Dependencies |
|----|------|--------------|
| A.1 | Create `agents/bulwark-issue-analyzer.md` with frontmatter | None |
| A.2 | Write Identity and Mission sections | A.1 |
| A.3 | Write Protocol section (6 steps) | A.2 |
| A.4 | Write Tool Usage Constraints section | A.3 |
| A.5 | Write Output Format section (reference skills) | A.4 |
| A.6 | Copy to `.claude/agents/` for testing | A.5 |
| A.7 | Test invocation via Task tool | A.6 |
| A.8 | Verify debug report schema compliance | A.7 |
| A.9 | Verify diagnostics output | A.7 |

---

## 6. Verification Plan

### 6.1 Structural Verification

```bash
# Check agent file exists
ls -la agents/bulwark-issue-analyzer.md

# Check frontmatter
head -20 agents/bulwark-issue-analyzer.md

# Verify skills exist
ls skills/issue-debugging/SKILL.md
ls skills/subagent-output-templating/SKILL.md
```

### 6.2 Functional Verification

| Test | Method | Expected |
|------|--------|----------|
| Agent invocation | `Task(subagent_type="bulwark-issue-analyzer", ...)` | Agent executes |
| Debug report created | Check `logs/debug-reports/` | YAML file with correct schema |
| Diagnostics created | Check `logs/diagnostics/` | YAML file with correct schema |
| Summary returned | Read Task output | Contains debug report path |
| No code modifications | Check git status | No changes to src/ or tests/ |

### 6.3 Test Fixture

Create a simple test fixture for verification:

```
tests/fixtures/issue-analyzer-test/
├── src/
│   └── buggy.ts          # File with known bug
├── tests/
│   └── buggy.test.ts     # Failing test
└── issue.md              # Issue description
```

---

## 7. Success Criteria

From tasks.yaml:

| Criterion | Verification |
|-----------|--------------|
| Custom sub-agent definition (markdown file) | File exists at `agents/bulwark-issue-analyzer.md` |
| Frontmatter: `agent: sonnet` | Check frontmatter |
| Frontmatter: `skills: issue-debugging` | Check frontmatter (also subagent-output-templating) |
| Produces debug report at `logs/debug-reports/` | Functional test |
| Summary includes debug report path | Functional test |
| Diagnostic output to `logs/diagnostics/` | Functional test |

---

## 8. Dependencies

| Dependency | Status |
|------------|--------|
| `issue-debugging` skill (P0.4) | Complete |
| `subagent-output-templating` skill (P0.2) | Complete |
| Timestamp format standardization | Pending (see `docs/timestamp-fix.md`) |

---

## 9. Risks & Mitigations

| Risk | Mitigation |
|------|------------|
| Agent modifies code despite constraints | Clear instruction-based constraints, test verification |
| Debug report schema drift | Reference fix-validation.md schema explicitly |
| Timestamp format confusion | Fix timestamp ambiguity before implementation (recommended) |
| Bash commands cause harm | Explicit forbidden command list in constraints |

---

## 10. Notes

- This is a **wrapper agent** that encapsulates the `issue-debugging` skill
- Unlike P1.1 (skipped), this agent doesn't need to spawn sub-agents internally
- The debug report is consumed by P1.3 (`bulwark-fix-validator`) later in the pipeline
- Consider fixing timestamp ambiguity (see `docs/timestamp-fix.md`) before implementation

---

## 11. Approval

- [ ] Task brief reviewed
- [ ] User approved to proceed
- [ ] Timestamp fix applied (recommended before implementation)
