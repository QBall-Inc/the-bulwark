# P5.13 — Plan-Creation Skill (Agent Teams Dual-Mode)

## Overview

| Field | Value |
|-------|-------|
| Task ID | P5.13 |
| Phase | P5 — Test Audit Hardening, Evolution Tools, Testing Protocol |
| Status | Not started |
| Estimated sessions | 3 |
| Dependencies | P5.3 (continuous-feedback) |
| Brainstorm outputs | `logs/brainstorm/plan-creation/` (5 agent outputs + synthesis) |
| Research inputs | `logs/research/agent-teams/synthesis.md` |

## Problem Statement

Create a plan-creation skill that produces structured implementation plans through a 4-role collaborative scrum team. The skill uses Claude Code's Task tool as the default orchestration mode and Agent Teams as an enhanced opt-in mode for users who want peer debate between team members.

### Distribution Priority

| Priority | Context | Role |
|----------|---------|------|
| P1 (highest) | CLEAR Framework (`/mnt/c/projects/clear-framework`) | Core workflow feature — produces the initial plan artifact that CLEAR's plan-management skill consumes |
| P2 | Essential Skills standalone repo | General-purpose plan-creation, no branding, portable |
| P3 (lowest) | Bulwark plugin | Optional add-on, Agent Teams proving ground |

Architecture must be portable across all 3 contexts. No hardcoded project paths. No CLEAR-specific dependencies.

## Functional Requirements

### FR1: Dual-Mode Orchestration

- **Task tool mode (default)**: PO first → [Architect, Eng+Delivery Lead] parallel → QA/Critic last
- **Agent Teams mode (enhanced, opt-in)**: PO first via Task tool → 3 teammates (Architect, Eng+Delivery Lead, QA/Critic) debate via mailbox with delegate-mode lead
- Mode detection: Pre-flight checks `$CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS` env var
- Agent Teams mode requires BOTH the env var AND explicit user opt-in (not automatic)
- Graceful degradation: if env var absent when Agent Teams requested, notify user and fall back to Task tool

### FR2: 4-Role Scrum Team

| Role | Focus | Execution |
|------|-------|-----------|
| **Product Owner** | Requirements, scope, acceptance criteria, user value | Solo first (codebase exploration via Task tool, both modes) |
| **Technical Architect** | System design, component decomposition, integration, trade-offs | Parallel (Task tool) / Teammate (Agent Teams) |
| **Engineering & Delivery Lead** | Implementation sequencing, effort estimation, scheduling, milestones, dependencies, risk | Parallel (Task tool) / Teammate (Agent Teams) |
| **QA / Critic** | Challenge assumptions, identify gaps, kill criteria, adversarial review | Sequential last (Task tool) / Active participant throughout (Agent Teams) |

Key design decisions:
- PO always runs via Task tool even in Agent Teams mode (codebase exploration is solo, no debate benefit)
- QA/Critic participates throughout in Agent Teams mode (not gated until end) — this is the primary quality advantage of Agent Teams
- Combined Eng+Delivery Lead addresses the 3-4 agent coordination cap from research while preserving distinct functional coverage

### FR3: Implementation Plan Output

Output is a **hybrid Markdown preamble + YAML body** plan:

```markdown
# {Project Name} — Implementation Plan

## Executive Summary
{2-3 paragraphs: goal, approach, key risks — prose for human readability}

## Plan
```yaml
version: "1.0"
project_name: "{name}"
created: "{ISO date}"
created_by: "plan-creation skill"
mode: "task-tool | agent-teams"

phases:
  - id: "phase_1"
    name: "{Phase Name}"
    status: "not_started"
    workpackages:
      - id: "WP1"
        name: "{Workpackage name}"
        description: "{What this delivers}"
        estimated_sessions: {N}
        dependencies: []
      - id: "WP2"
        name: "{Workpackage name}"
        dependencies: ["WP1"]

milestones:
  - id: "m1"
    name: "{Milestone name}"
    phase: "phase_1"
    type: "major"
    requires: ["WP1", "WP2"]

dependency_graph:
  critical_path: ["WP1", "WP2", "WP5"]
  parallel_opportunities:
    - ["WP3", "WP4"]

risks:
  - id: "R1"
    description: "{Risk}"
    severity: "high | medium | low"
    mitigation: "{Mitigation}"

kill_criteria:
  - "{Condition under which project should be abandoned}"
```
```

**CLEAR compatibility**: YAML body field names match CLEAR's `MasterPlan` type interface (`phases[].id`, `phases[].status`, `milestones[].type`, `milestones[].requires`) without importing CLEAR types. When CLEAR resumes, its `parseMasterPlanContent()` can consume the YAML body with minor field mapping.

**Token efficiency**: YAML body for a 3-phase, 12-workpackage plan is ~400-600 tokens vs ~800-1200 for equivalent Markdown tables.

### FR4: SA2 Compliance

- **Task tool mode**: All agent output to `logs/plan-creation/{slug}/{NN}-{role}.md` (standard SA2)
- **Agent Teams mode**: Dual-output contract in every teammate prompt:
  - Full analysis → `logs/plan-creation/{slug}/{NN}-{role}.md` (SA2 artifact)
  - 3-5 sentence coordination summary → mailbox (Agent Teams peer messaging)
- Synthesis stage verifies all log files exist before proceeding

### FR5: Explore-Then-Commit Workflow

Borrowed from Claude Code's plan mode concept:
1. Agents research and produce analysis (explore phase)
2. Orchestrator synthesizes into draft plan
3. User reviews draft via AskUserQuestion approval gate
4. On approval, plan written to user-specified location (or default `plans/`)
5. Critical Evaluation Gate applied to user feedback (Preference/Technical Claim/Architectural Suggestion)

### FR6: Portability

- Self-contained directory: `skills/plan-creation/SKILL.md` + `references/` + `templates/`
- No hardcoded project paths — PO explores codebase autonomously (same as brainstorm SME)
- Only skill dependency: `subagent-prompting` (declared in frontmatter)
- Works in any project with no Bulwark or CLEAR infrastructure

## Technical Design

### Architecture: Sandwich Structure

```
Stage 1: Pre-Flight (SHARED)
├── Parse input (topic/document + optional research file)
├── Slugify topic, create logs/plan-creation/{slug}/
├── Detect mode: check env var + user preference
├── AskUserQuestion if ambiguous (2-3 questions per round)
├── Load subagent-prompting skill
├── Token budget check (warn if >30%)
└── Load references/role-product-owner.md

Stage 2: Context Gathering (SHARED)
├── Spawn PO agent (Opus, Task tool in both modes)
├── PO explores codebase autonomously
├── PO output: logs/plan-creation/{slug}/01-product-owner.md
└── Token budget check

Stage 3A: Scrum Team — Task Tool Mode
├── Spawn [Architect, Eng+Delivery Lead] in parallel (2 Task tool calls)
├── Each reads PO output + problem statement + research synthesis
├── Each writes to logs/plan-creation/{slug}/{NN}-{role}.md
├── Then spawn QA/Critic sequentially with all prior outputs
└── Token budget check (checkpoint if >55%)

Stage 3B: Scrum Team — Agent Teams Mode
├── Lead enters delegate mode (coordination only)
├── Spawn 3 teammates: Architect, Eng+Delivery Lead, QA/Critic
│   └── Model class: Opus (default) or Sonnet (user choice from AT Confirmation Flow)
├── Each prompt includes:
│   ├── Dual-output contract (logs + mailbox)
│   ├── CC-to-lead instruction: "When sending peer DMs with work or findings, also send 1-line summary to Scrum Lead"
│   └── Rendezvous instruction: "Final action: send Scrum Lead 'WORK COMPLETE — all tasks done, log written to [path]'"
├── Peer debate: explicit challenge prompts between teammates
├── QA/Critic participates throughout (active adversarial role)
├── In-process display mode (WSL2 safe)
├── Shutdown gate: Lead MUST NOT call requestShutdown until:
│   ├── All shared task list tasks are in terminal state (completed/blocked)
│   └── WORK COMPLETE message received from ALL 3 teammates
└── Token budget check

Stage 4: Synthesis (SHARED, mode-aware)
├── Task tool: read all 4 log files
├── Agent Teams: read all 4 log files + lead coordination notes
├── Write draft plan using plan output template
├── AskUserQuestion approval gate (explore-then-commit)
├── Critical Evaluation Gate on user responses
└── Write final plan to user-specified location

Stage 5: Diagnostics (SHARED)
└── Write diagnostic YAML to logs/diagnostics/
```

### F# Pipeline Syntax

```fsharp
// Task tool mode
ProductOwner(topic, research?)
|> [Architect, EngDeliveryLead](po_output)
|> QACritic(all_prior_outputs)
|> Synthesis
|> ApprovalGate

// Agent Teams mode
ProductOwner(topic, research?)            // Task tool (solo)
|> AgentTeam[Architect, EngDeliveryLead, QACritic](po_output)  // Peer debate
|> Synthesis
|> ApprovalGate
```

### Skill Directory Structure

```
skills/plan-creation/
├── SKILL.md
├── references/
│   ├── role-product-owner.md
│   ├── role-technical-architect.md
│   ├── role-eng-delivery-lead.md
│   └── role-qa-critic.md
└── templates/
    ├── role-output.md
    ├── plan-output.md
    ├── critic-output.md
    ├── synthesis-output.md
    └── diagnostic-output.yaml
```

### Frontmatter

```yaml
name: plan-creation
description: "Create structured implementation plans using a 4-role scrum team with optional Agent Teams peer debate"
user-invocable: true
skills:
  - subagent-prompting
```

### Mode Detection Logic (Pre-Flight)

```
1. Check $CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS env var
2. If env var is SET:
   - Offer user choice: "Agent Teams enhanced mode is available. Use Agent Teams or Task tool?"
   - Default to Task tool if user doesn't specify
3. If env var is NOT SET:
   - Use Task tool mode
   - If user explicitly requested Agent Teams: notify "Agent Teams requires CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1. Using Task tool mode."
```

### AT Confirmation Flow (Pre-Flight, after user selects Agent Teams)

When the user selects Agent Teams in the mode detection step above, execute this confirmation flow before spawning any teammates:

**Step 1: Display RED warning banner**

Use ANSI color `\033[38;2;255;154;150m` (#FF9A96, pastel coral — matches ez-statusline GAUGE_HIGH):

```
⚠️  NOTICE: Claude Code's Agent Teams is an experimental feature. Unexpected
issues like teammates being stuck or unresponsive may occur. Agent Teams mode
is also significantly more token-expensive than Task tool mode (4 concurrent
agents vs sequential sub-agents).

If you run into issues, start a new session and re-run /plan-creation.
If the final output does not match expectations, re-run with Task tool mode.
```

**Step 2: AskUserQuestion — model class + mode confirmation**

Present a single question with 3 options:

| Option | Label | Description |
|--------|-------|-------------|
| 1 | Opus agents (Recommended) | Higher quality analysis, highest token cost. 4 Opus-class agents for all roles. |
| 2 | Sonnet agents | Good quality, lower token cost. 4 Sonnet-class agents for all roles. |
| 3 | Switch to Task tool mode | Cancel Agent Teams and use sequential sub-agents instead. |

- If user selects option 1: proceed with Opus agents in AT mode
- If user selects option 2: proceed with Sonnet agents in AT mode
- If user selects option 3: fall back to Task tool mode (Stage 3A), skip AT entirely

**Step 3 (AT completion): Display AMBER banner**

After AT execution completes successfully and the synthesis/plan is presented, display an amber banner using ANSI color `\033[38;2;255;244;176m` (#FFF4B0, pastel yellow — matches ez-statusline GAUGE_MID):

```
ℹ️  Agent Teams is an experimental feature. If the final plan does not match
your expectations, try re-running /plan-creation with Task tool mode
(sequential sub-agents). Individual role outputs are preserved in
logs/plan-creation/{slug}/ for inspection.
```

## TC2 Learnings (Session 78 — dev-team AT validation)

TC2 tested Agent Teams mechanics using a dev-team skill in PM-Essentials (URL shortener CLI task, 3 teammates: Developer, Security Reviewer, Tester). AT pattern validated — mailbox, peer debate, dual-output all work. Three issues discovered that directly inform Stage 3B design:

### Issue 1: False Stall Detection

**Observed:** Lead perceived Developer as idle and sent nudge messages while Developer was actively writing code. The Developer was busy with a long-running task (code generation) and couldn't respond to Lead messages until the current tool call completed.

**Root cause:** Lead has no visibility into teammate activity state between messages. No heartbeat, no "is busy" poll. If a teammate is executing a tool call (writing code, running tests), the Lead sees silence and may interpret it as a stall.

**Mitigation in plan-creation:** Replace time-based "idle = stall" heuristic with task-list-based detection. A teammate is stalled only if its claimed task has been `in_progress` for extended time AND no new tasks claimed AND no message received. Teammates instructed to update shared task list when accepting peer-dispatched work.

### Issue 2: Peer DM Blind Spot

**Observed:** Security Reviewer and Tester communicated directly via peer DM. Lead had no visibility into this exchange. Lead incorrectly assessed both as idle when they were actively collaborating.

**Root cause:** Peer-to-peer messages go to the target's inbox only — the Lead's inbox does not receive a copy. This is an architectural constraint of AT, not configurable. Confirmed by both official docs and empirical testing.

**Mitigation in plan-creation:** CC-to-lead prompt instruction in every teammate spawn: "When sending peer DMs with work instructions or significant findings, also send a 1-line summary to the Scrum Lead." Behavioral instruction — compliance not guaranteed but low-cost.

### Issue 3: Premature Shutdown

**Observed:** Lead concluded all work was done and sent shutdown requests to all teammates. Security Reviewer complied and shut down. But Tester was actively executing a second round of tests based on edge cases received from Security Reviewer via peer DM. Lead kept trying to shut down Tester while Tester was mid-execution. Required user intervention to stop the shutdown attempts.

**Root cause:** Compound of Issues 1 + 2. Lead couldn't see the peer DM handoff (Issue 2), and couldn't see the Tester was actively working (Issue 1). Led to premature shutdown that interrupted in-flight work.

**Mitigation in plan-creation:** Explicit rendezvous gate. Every teammate's spawn prompt includes: "Your final action before going idle is to send the Scrum Lead: 'WORK COMPLETE — all tasks done, log written to [path]'." Lead MUST NOT call requestShutdown until WORK COMPLETE received from ALL teammates AND all shared tasks are terminal.

### Summary: Three Mitigation Patterns Applied to Stage 3B

| Pattern | Mechanism | Where Applied |
|---------|-----------|---------------|
| **A: CC-to-lead** | Prompt instruction to summarize peer DMs to Lead | All teammate spawn prompts |
| **B: Task list as coordination layer** | Teammates create tasks for peer-dispatched work | All teammate spawn prompts |
| **C: Rendezvous gate** | WORK COMPLETE message required before shutdown | Lead shutdown logic in SKILL.md |

## Success Criteria

### Functional (both modes)

1. Skill produces a structured implementation plan with phases, workpackages, milestones, and dependencies
2. All 4 scrum team roles contribute distinct, non-redundant perspectives visible in log artifacts
3. QA/Critic identifies at least one substantive gap not raised by other roles
4. YAML body is structurally compatible with CLEAR's MasterPlan interface
5. Passes `/anthropic-validator` with 0 critical, 0 high

### Task tool mode

6. SA2 compliance: all output in `logs/plan-creation/{slug}/`
7. Follows bulwark-brainstorm patterns (4-part prompting, diagnostics, error handling)

### Agent Teams mode

8. Graceful degradation when env var absent (user notification, Task tool fallback)
9. SA2 compliance: artifacts in logs/, mailbox for coordination only
10. Observable peer challenge in Agent Teams mode (teammate X challenges teammate Y)
11. RED warning banner displayed before AT confirmation (ANSI #FF9A96)
12. AskUserQuestion offers model class choice (Opus/Sonnet) and Task tool fallback
13. AMBER informational banner displayed after AT completion (ANSI #FFF4B0)
14. No premature shutdown — WORK COMPLETE rendezvous gate respected

### Portability

11. Skill runs in a fresh project (no Bulwark, no CLEAR) with no errors
12. Rsync to Essential Skills repo succeeds and skill remains functional

## Verification Plan

### Manual Test Protocol (Session 3)

**Task Tool Mode:**
1. Invoke skill with real topic and research file, env var NOT set
2. Verify PO spawns first, explores codebase autonomously
3. Verify Architect + Eng+Delivery Lead spawn in parallel
4. Verify QA/Critic spawns last with all prior outputs
5. Verify all 4 log files exist in `logs/plan-creation/{slug}/`
6. Verify plan output has Markdown preamble + YAML body
7. Verify YAML body has phases, workpackages, milestones, dependencies
8. Verify diagnostic YAML written

**Agent Teams Mode:**
1. Set `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`
2. Invoke skill, select Agent Teams when offered
3. Verify RED warning banner displayed with correct color (#FF9A96)
4. Verify AskUserQuestion presents 3 options: Opus agents / Sonnet agents / Switch to Task tool
5. Select Opus (or Sonnet) — verify correct model class used for teammates
6. Verify delegate mode active (lead coordinates only)
7. Verify 3 teammates spawn with correct roles
8. Verify peer challenge occurs via mailbox
9. Verify CC-to-lead pattern: lead receives summaries of peer DMs
10. Verify all 4 SA2-compliant log files exist
11. Verify shutdown waits for WORK COMPLETE from all teammates (no premature shutdown)
12. Verify synthesis incorporates debate insights
13. Verify AMBER banner displayed after completion with correct color (#FFF4B0)
14. Verify diagnostic YAML captures AT-specific metrics
15. Verify no orphaned processes after completion

**Agent Teams Fallback Test:**
1. At AT confirmation step (Step 4 above), select "Switch to Task tool mode"
2. Verify skill falls back to Stage 3A (Task tool mode) with no AT infrastructure spawned

**Cross-Mode Comparison (if time permits):**
- Same topic through both modes, compare output quality and token cost

**Validation:**
- Run `/anthropic-validator` — 0 critical, 0 high
- Portability test in fresh project directory

## Kill Criteria (from Critical Analyst)

1. Comparative test shows no quality difference between Agent Teams and Task tool → descope Agent Teams mode
2. 3+ of 4 agents produce overlapping analysis → reduce roles further
3. Agent Teams fails to produce all 4 SA2-compliant log files → kill Agent Teams mode
4. Agent Teams experimental flag removed or API changes before Session 2 → defer Agent Teams indefinitely
5. Total implementation exceeds 3 sessions → ship whatever works at Session 3 boundary

## Build Order

| Session | Scope | Risk | Key Deliverables |
|---------|-------|------|------------------|
| **Session 1** | Task tool mode + all scaffolding | Low (80% brainstorm reuse) | SKILL.md, 4 role references, templates, Task tool execution flow, dogfood copy |
| **Session 2** | Agent Teams mode layered on top | Medium (first-ever AT in Bulwark) | AT execution flow in SKILL.md, teammate prompts with dual-output, graceful degradation, sync script update |
| **Session 3** | Manual testing + validation | Low-Medium | Test protocol, both mode test runs, anthropic-validator, fix issues |

**De-risk strategy**: Start Session 1 with role reference files (most uncertain component). Task tool mode first provides permanent fallback.

## Reuse from bulwark-brainstorm

| Component | Reuse | Notes |
|-----------|-------|-------|
| SKILL.md structure (stages, checklists) | 90% | Rename stages, swap roles |
| Pre-flight stage | 85% | Add env var detection + mode choice |
| AskUserQuestion protocol | 100% | Identical |
| Critical Evaluation Gate | 100% | Identical |
| Token budget management | 100% | Same thresholds |
| Error handling | 90% | Add AT failure modes |
| Diagnostic YAML template | 80% | Add AT metrics |
| Role reference file format | 70% | Same structure, new content |
| Synthesis template | 50% | Plan format differs from brainstorm |
| Execution flow | 30% | Task tool similar, AT entirely new |

## Post-Brainstorm Deliverable: CLEAR Adapter Doc

After plan-creation skill is built, create a brief adapter-layer document at `plans/task-briefs/CLEAR-plan-adapter-notes.md` for use when CLEAR Framework development resumes. This doc should specify:
- How CLEAR's `parseMasterPlanContent()` maps to plan-creation's YAML output
- Field mapping differences (inline workpackage objects vs string ID references)
- Integration point: CLEAR's `/cf-plan create` consuming plan-creation output

## References

| Resource | Relevance |
|----------|-----------|
| `logs/research/agent-teams/synthesis.md` | Agent Teams research — 5 viewpoints, P5.13 HIGH applicability |
| `logs/brainstorm/plan-creation/synthesis.md` | Brainstorm synthesis with post-synthesis decisions |
| `skills/bulwark-brainstorm/SKILL.md` | Direct structural template |
| `plans/references.md` | Agent Teams external references |
| `/mnt/c/projects/clear-framework/src/infrastructure/plan/types.ts` | CLEAR MasterPlan interface for format compatibility |
| `/mnt/c/projects/clear-framework/briefs/core/plan-management-feature-brief.md` | CLEAR plan infrastructure scope boundary |
| PM-Essentials `logs/dev-team/url-shortener-cli/synthesis.md` | TC2 empirical AT validation — peer DM blind spot, false stall detection, premature shutdown |
| PM-Essentials `logs/diagnostics/dev-team-20260224-004800.yaml` | TC2 diagnostic data — 3 stall interventions, 12 peer messages, 3 debates |
| PM-Essentials `.claude/skills/dev-team/SKILL.md` | AT test skill used for TC2 — patterns to improve upon |
